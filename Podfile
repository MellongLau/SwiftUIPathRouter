# Uncomment the next line to define a global platform for your project
# platform :ios, '16.0'

target 'SwiftUIPathRouter' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  
  pod 'Sourcery'

  # Pods for SwiftUIPathRouter

  target 'SwiftUIPathRouterTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SwiftUIPathRouterUITests' do
    # Pods for testing
  end

end
